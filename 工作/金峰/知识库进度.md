# 企业微信知识库机器人进度

> 项目目录：`C:\Users\DC\Documents\ChatGPT\机器人`  
> 只读知识源：`C:\Users\DC\Documents\GitHub\Study_Notes`  
> 完整执行规格：`企业微信知识库机器人-执行Prompt.md`  
> 本记录最后更新：2026-09-04（Asia/Shanghai）

## 当前状态

- 总体状态：实施中
- 当前阶段：阶段 7 离线验收通过，真实模型与企微外部验收待配置；准备进入阶段 8
- 已完成：阶段 -1～4；阶段 5～6 代码完成；多模型路由、熔断、权限、安全和健康检查回归通过
- 当前阻塞：真实 embedding 凭证未配置，当前使用 mock embedding；不阻塞后续代码和离线测试
- 下一步：Windows 长期运行、备份恢复和运行手册

## 任务步骤

- [x] 阶段 -1：建立长程状态与恢复协议
  - 创建 `docs/execution-state.json`、`docs/progress.md`、`docs/decisions/`、`docs/failure-log.md`、`artifacts/gates/`、`config/gates.yaml`
  - 写入执行规格哈希，支持原子状态更新、恢复检查和失败重试计数
  - 提供状态查看命令，并验证中断恢复流程

- [x] 阶段 0：完成勘察与基线
  - 检查 Git、Python、uv、Docker、端口和项目环境
  - 以只读方式统计知识源，抽样检查 Markdown、PDF、TXT
  - 定义纳入/排除规则、敏感目录默认权限和环境变量占位配置

- [x] 阶段 1：建立工程骨架与可重复运行环境
  - 创建 Python/uv 工程结构、统一配置、结构化日志和脱敏机制
  - 实现 `/health/live`、`/health/ready` 及相关测试
  - 完善 README、启动方式、测试命令和 `.gitignore`

- [x] 阶段 2：实现文档解析与增量索引
  - 支持 Markdown、PDF、TXT 解析、语义切分和来源追溯
  - 用 SQLite 管理文件状态，用 Qdrant 管理版本化向量索引
  - 支持 dry-run、全量、增量和状态查询；建立语料快照清单

- [x] 阶段 3：实现混合检索与重排
  - 组合中文关键词检索、向量检索、融合排序与可降级重排
  - 实现路径、标签、目录和访问级别过滤
  - 建立不少于 50 条检索评测，达到 `Recall@5 >= 0.85`

- [x] 阶段 4：实现基于证据的回答链与会话上下文
  - 实现查询改写、证据阈值、引用校验、无依据拒答和冲突展示
  - 隔离单聊、群聊和用户上下文，支持 TTL、追问重检索和清除上下文
  - 防御文档提示注入，确保回复不泄漏本机绝对路径

- [ ] 阶段 5：接入企业微信 API 模式智能机器人（代码完成，20 条真实 E2E 待配置）
  - 依据当前官方文档实现长连接传输适配器，不使用单向群机器人 Webhook 代替接收能力
  - 支持单聊、群聊 @、幂等去重、限流、排队、超时和安全分段
  - 凭证未就绪时先完成 mock 测试；就绪后完成 20 条真实端到端测试

- [ ] 阶段 6：完善权限、安全与可观测性（代码完成，真实模型故障切换待配置）
  - 建立用户/群聊授权映射及检索前、生成后的双重权限校验
  - 增加速率限制、并发上限、超时、熔断、隐私化日志和指标
  - 完成越权、敏感信息、注入、超长输入、压力及串话测试

- [ ] 阶段 7：建立质量评测与持续改进机制（离线门禁通过，真实模型语义评测待配置）
  - 建立至少 100 条、绑定语料快照的评测样本及分级复核状态
  - 分离调优集和冻结测试集，保存可复现基线报告
  - 达到召回、引用支持率、拒答准确率、权限和注入防护硬门槛

- [ ] 阶段 8：实现运行、部署与自动恢复
  - 提供一键启动、停止、状态和日志命令
  - 实现进程守护、每日增量索引、失败保留旧索引、备份与启动自检
  - 编写覆盖常见故障的运行手册并验证恢复流程

- [ ] 阶段 9：执行最终验收与交付
  - 从干净环境完成安装、启动、全量/增量索引及全部门禁
  - 完成离线质量、安全、故障恢复和企业微信真实端到端验收
  - 扫描凭证、路径及正文泄漏，核对执行状态与实际现场一致
  - 仅在全部硬性门槛通过后标记完成；缺少外部凭证时标记“代码完成，外部验收待配置”

## 更新规则

1. 每次出现实质进展时，同步更新“当前状态”、对应任务复选框和下方“进度日志”。
2. 阶段只有在其门禁命令、必需产物和通过条件全部满足，并已有验证记录后，才能勾选完成。
3. 测试失败、技术决策、阻塞项和恢复入口也必须记录，不能只记录成功事项。
4. 本文件是面向人的进度总览；项目内 `docs/execution-state.json` 和 `docs/progress.md` 建立后，与其保持一致，以机器验证结果为准。
5. 不在本文件中写入凭证、令牌、知识库敏感正文或其他秘密。

## 进度日志

### 2026-09-04

- 完成内容：创建并初始化知识库工程进度记录；从完整执行规格提炼阶段 -1～9 的任务清单。
- 修改文件：`工作/金峰/知识库进度.md`
- 验证结果：已确认文件位于实际知识源目录 `C:\Users\DC\Documents\GitHub\Study_Notes`；工程实施尚未开始，未运行阶段门禁。
- 未解决风险：尚未建立项目内机器可读状态与门禁基础设施。
- 下一步：实施并验收阶段 -1。

### 2026-09-04 10:48（阶段 2 验收）

- 完成内容：保留并核验既有阶段 -1～1；完成 Markdown/PDF/TXT 解析、SQLite 增量清单、Qdrant 版本索引、full/incremental/dry-run/status 命令及上一版保留。
- 修改文件：项目 `ingestion/`、`tests/test_ingestion.py`、`tests/test_pipeline.py`、`data/corpus-manifest.json`、`artifacts/gates/stage-2-*.json`、三份进度状态文件。
- 验证命令：`.venv/Scripts/python -m pytest tests -q`（32 passed）；`.venv/Scripts/python -m ingestion.cli index --status`（v4、341 文件、1371 chunks）；无变化 incremental（0 embedding calls）。
- 测试结果：合格文件处理覆盖率 100%，340 个解析成功，1 个扫描 PDF 标记 `ocr_required`，0 个失败。
- 失败原因：本次无新增失败；历史阶段 2 的针对性修复详见项目 `docs/failure-log.md`。
- 遗留风险：当前为 mock embedding，真实语义质量和外部服务可用性待本机 `.env` 配置后验收；1 个扫描 PDF 待 OCR。
- 下一步：阶段 3 混合检索与 Reranker。

### 2026-09-04 11:07（阶段 2 整改复验 + 阶段 3 验收）

- 完成内容：排除第三方源码/缓存/构建目录和运维进度文件；全量索引改为 Qdrant 新集合完成后原子替换 SQLite 清单与指针；实现中文关键词 + 向量 + RRF、权限前置过滤、Reranker 降级、去重和固定证据包。
- 修改文件：项目 `config/exclude.txt`、`ingestion/registry.py`、`ingestion/pipeline.py`、`ingestion/vectorstore.py`、`retrieval/`、`evals/retrieval_*`、相关测试/门禁/状态文件。
- 验证命令：阶段 2 full + no-change incremental + status；阶段 3 60 条 retrieval eval；完整 pytest。
- 测试结果：索引 v7，156 文件、900 chunks、覆盖率 100%、第三方源码路径 0、无变化 embedding 0；Recall@5=0.95；36 passed。
- 失败原因：旧范围规则遗漏 `源码/`；旧 full 不清理 SQLite 残留；评测曾误判镜像等价来源。均按 `docs/failure-log.md` 做最小修复并留痕。
- 遗留风险：仍为 mock embedding；真实 embedding 配置后需重建和复评；1 个扫描 PDF 待 OCR。
- 下一步：阶段 4 证据回答与引用校验。

### 2026-09-04 11:13（阶段 4 验收）

- 完成内容：固定证据包、证据不足前置拒答、统一生成接口、逐句引用/来源存在性/权限二次校验、提示注入数据边界、会话隔离与清除。
- 修改文件：项目 `generation/`、`tests/test_generation.py`、`app/config.py`、门禁和进度文件。
- 验证命令及结果：阶段 4 单测 8 passed；完整 pytest 44 passed。
- 失败原因：首次句子切分误把句末后的 `[S1]` 与前句分离，最小修复后通过，详见 failure-log #11。
- 遗留风险：语义蕴含与冲突评测待阶段 7；真实模型尚未配置。
- 下一步：阶段 5 企业微信与 RAG 串联。

### 2026-09-04 11:20（阶段 5 代码验收）

- 完成内容：保留官方 WebSocket，新增 Node→Python RAG API、内部令牌、取消、哈希会话键、跨重启去重、非 @ 群消息过滤、UTF-8 分段。
- 修改文件：项目 `src/index.js`、`src/rag-client.js`、`src/dedupe.js`、`src/message-policy.js`、`app/main.py` 及相关测试/配置。
- 验证命令及结果：`npm test` 9 passed；`npm run check` 通过；Python answer API 1 passed。
- 失败原因：无新增失败。
- 遗留风险：20 条真实企微 E2E 尚未执行，真实租户 @ 标志需现场验证；因此阶段 5 未作外部完成声明。
- 下一步：阶段 6 多模型、熔断、权限与可观测性。

### 2026-09-04 11:38（阶段 6 代码验收）

- 完成内容：OpenAI/DeepSeek/Mock 统一 Provider；顺序路由、错误分类、配置故障、熔断半开、总预算；哈希权限映射、限流并发、脱敏日志、指标和完整健康状态。
- 修改文件：项目 `generation/`、`security/`、`observability/`、`app/`、权限/环境占位配置及测试。
- 验证命令及结果：路由/安全/API 15 passed；Python 全量 60 passed；Node 10 passed + 语法检查通过。
- 失败原因：一次旧健康键测试漂移，最小更新后通过，见 failure-log #12。
- 遗留风险：真实 OpenAI/DeepSeek 调用和故障切换待本机凭证；已暴露疑似密钥需先轮换；同步 provider 的调用中取消能力有限。
- 下一步：阶段 7 质量评测。

### 2026-09-04 11:50（阶段 7 离线验收）

- 完成内容：120 条 tuning/frozen 质量安全集和 60 条检索集，全部绑定当前语料版本；报告不含知识正文。
- 修改文件：项目 `evals/quality_*`、`evals/retrieval_cases.json`、门禁报告、测试和状态文件。
- 验证命令及结果：质量 120 条（引用/拒答/权限/注入均 1.0）；Recall@5=0.95；Python 61 passed、Node 10 passed。
- 失败原因：首版无答案 fixture 含常见英文 n-gram，误触发弱匹配；只替换 fixture 后通过，见 failure-log #13。
- 遗留风险：Mock/结构契约不能替代真实模型语义质量；真实模型和 20 条企微 E2E 待配置。
- 下一步：阶段 8 长期运行与恢复。
